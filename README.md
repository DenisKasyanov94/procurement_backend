procurement_backend/          # Корневая директория проекта (репозиторий Git)
├── .git/                     # Служебная папка Git (создастся автоматически)
├── .gitignore                # Файл, указывающий Git, какие файлы/папки игнорировать
├── .env                      # Файл для хранения секретных данных (токены, пароли БД)
├── requirements.txt          # Список зависимостей Python (библиотеки)
├── README.md                 # Описание проекта
├── manage.py                 # Скрипт для управления Django-проектом
├── procurement_backend/      # Главный пакет проекта (settings.py, urls.py)
│   ├── __init__.py
│   ├── settings.py          # Основные настройки Django (БД, приложения, middleware)
│   ├── urls.py              # Корневой маршрутизатор URL для всего проекта
│   ├── asgi.py              # Точка входа для ASGI-серверов (для асинхронности)
│   └── wsgi.py              # Точка входа для WSGI-серверов (деплой)
└── backend/                  # Наше основное приложение (здесь будут модели, views, API)
    ├── __init__.py
    ├── admin.py             # Регистрация моделей в админке Django
    ├── apps.py              # Конфигурация приложения
    ├── models.py            # ОПРЕДЕЛЕНИЕ МОДЕЛЕЙ ДАННЫХ (наш главный файл сегодня)
    ├── views.py             # Функции/классы, обрабатывающие запросы (будущая логика API)
    ├── serializers.py       # Классы для преобразования моделей в JSON и обратно (для DRF)
    ├── urls.py              # Маршрутизатор URL конкретно для приложения backend
    ├── migrations/          # Папка для миграций БД (создастся автоматически)
    │   └── __init__.py
    ├── tests.py
    └── management/          # Кастомные команды Django (например, для импорта)
        └── commands/
            └── import_data.py